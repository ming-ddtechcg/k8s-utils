apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Values.redis.name }}
  annotations:
    resource-type: redis
    resource-name: {{ .Values.redis.name }}
  labels:
    app: {{ .Values.redis.name }}
  namespace: {{ .Release.Namespace }}
spec:
  serviceName: {{ .Values.redis.name }}
  replicas: {{ .Values.redis.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.redis.name }}
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      annotations:
        resource-type: redis-pod
      labels:
        app: {{ .Values.redis.name }}
        app-client: {{ .Values.redis.name }}
    spec:
{{- if .Values.service.nodeAffinity }}
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
{{- if .Values.service.nodeAffinity.key }}
{{- if eq .Values.service.nodeAffinity.value "enabled" }}
              - key: {{ .Values.service.nodeAffinity.key }}
                operator: In
                values:
                - {{ .Values.service.nodeAffinity.value }}
{{- else }}
              - key: no-node-label
                operator: In
                values:
                - DoesNotExist
{{- end }}
{{- else }}
              - key: node-role.kubernetes.io/control-plane
                operator: DoesNotExist
              - key: node-role.kubernetes.io/master
                operator: DoesNotExist 
{{- end }}
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - {{ .Values.redis.name }}
              topologyKey: kubernetes.io/hostname
{{- end }}
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: {{ .Values.redis.name }}
      containers:
      - command:
        - sh
        - -c
        - "mkdir -p /usr/local/sbin /usr/local/tmp; cp /usr/local/tmp-sbin/* /usr/local/sbin; cp /usr/local/tmp-utils-sbin/* /usr/local/sbin; chmod +x /usr/local/sbin/*.sh; /usr/local/sbin/redis-init.sh; /usr/local/sbin/redis.sh"
        env:
        - name: CHART_NAME
          value: {{ .Chart.Name }}
        - name: CHART_VERSION
          value: {{ .Chart.Version }}
        - name: POD_NAME
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: metadata.name
        - name: RUNNING_NODENAME
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: spec.nodeName
        - name: RUNNING_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: REDIS_CONFIG
          value: {{ .Values.redis.config.name }}
        name: {{ .Values.redis.name }}
        image: {{ .Values.redis.image }}
        imagePullPolicy: {{ .Values.redis.imagePullPolicy }}
        ports:
        - containerPort: {{ .Values.redis.containerport }}
        resources:
          limits:
            cpu: {{ .Values.redis.resources.limits.cpu }}
            memory: {{ .Values.redis.resources.limits.memory }}
          requests:
            cpu: {{ .Values.redis.resources.requests.cpu }}
            memory: {{ .Values.redis.resources.requests.memory }} 
        volumeMounts:
        - mountPath: /data
          name: redis-db
        - mountPath: /usr/local/tmp-sbin
          name: scripts
      serviceAccount: {{ .Values.service.name }}
      serviceAccountName: {{ .Values.service.name }}
      volumes:
      - name: scripts
        configMap:
          name: {{ .Values.redis.name }}-scripts
  volumeClaimTemplates:
  - metadata:
      name: redis-db
    spec:
      accessModes:
{{- range .Values.redis.storage.accessmodes }}
        - {{ . }}
{{- end }}
      storageClassName: {{ .Values.redis.storage.storageclass }}
      resources:
        requests:
          storage: {{ .Values.redis.storage.size }}


apiVersion: v1
kind: Service
metadata:
  name: test-webservice
  namespace: test-webservice
spec:
  selector:
    app: test-webservice
  ports:
    - protocol: TCP
      port: 80 
      targetPort: 80
  type: ClusterIP 
